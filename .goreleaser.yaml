version: 2

project_name: vpn-kongtrol

before:
  hooks:
    - go mod tidy

builds:
  # CLI binary — CGO disabled, true cross-compilation.
  - id: kongtrol-cli
    binary: kongtrol
    main: ./cmd/kongtrol
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - id: cli-archive
    builds: [kongtrol-cli]
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: "kongtrol_{{ .Os }}_{{ .Arch }}"
    files:
      - configs/example.yaml
      - README.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  draft: true
  replace_existing_draft: true

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
